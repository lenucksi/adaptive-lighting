on:
  push:
    branches: [main]
name: TOC Generator
jobs:
  generateTOC:
    name: TOC Generator
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
        with:
          TOC_TITLE: ""
